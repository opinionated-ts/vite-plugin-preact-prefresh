# Testing

<!--
# HUMAN-ONLY: AI agents MUST ignore the content inside this comment.

Document only testing rules and decisions that cannot be reliably inferred from
the existing tests, configuration, codebase, or other project documentation.

Include when applicable:

- **Testing principles** — behavior over implementation details, determinism, isolation, and meaningful coverage.
- **Test-level rules** — when to use unit, integration, or E2E tests when the choice is not obvious from the code.
- **Mocking rules** — what should or should not be mocked, especially at architectural boundaries.
- **External services** — how external dependencies must be isolated in tests.
- **Test data rules** — important requirements for fixtures, factories, builders, or test isolation.
- **Determinism rules** — required handling of time, randomness, environment, async operations, or other nondeterministic behavior.
- **Persistence rules** — required isolation and testing strategy for databases or other persistent state.
- **Coverage requirements** — minimum thresholds or other project-specific coverage policies.
- **Regression requirements** — when bug fixes must include regression tests.
- **Architectural testing boundaries** — components or boundaries that must be tested through public interfaces or integration tests.
- **Testing constraints** — rules that must not be violated.

Do not document test commands, framework configuration, file structure, naming
conventions, or patterns that can be inferred from the codebase or
`COMMANDS.md`, `CONVENTIONS.md`, or existing tests.

Keep this document focused on **testing intent, non-obvious rules, requirements,
and constraints**.
-->

## Related Documentation

- [Context](CONTEXT.md)
- [Commands](COMMANDS.md)
- [Workflow](WORKFLOW.md)
- [Architecture](ARCHITECTURE.md)
- [Conventions](CONVENTIONS.md)
